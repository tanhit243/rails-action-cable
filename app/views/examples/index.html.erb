<h1>Action Cable Examples</h1>

<p>
  Hello
  <%= render partial: 'partials/avatar', locals: { user: current_user } %>
  <%= current_user.name %>
  (<%= link_to 'Logout', destroy_user_session_path, method: :delete %>)!
</p>

<ul>
  <li><%= link_to 'Messages with live comments', messages_path %></li>
</ul>
